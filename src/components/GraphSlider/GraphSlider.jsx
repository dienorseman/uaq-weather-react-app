export const GraphSlider = ( { xAxislength, setLimits } ) => {
    return (
        <div>
            {/* <button
                onClick={() => {
                    setLimits((prevState) => {
                        if (prevState[1] + 30 > xAxislength) {
                            return prevState;
                        } else {
                            return [prevState[0] + 30, prevState[1] + 30];
                        }
                    });

                    console.log(limits);
                }}
            >
                move right
            </button> */}
        </div>
    );
};
